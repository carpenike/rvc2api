# Development Environment Variables for Frontend
# These are loaded in development mode

# Backend API base URL - uses localhost for development
VITE_API_URL=http://localhost:8000

# WebSocket base URL - uses localhost for development
VITE_WS_URL=ws://localhost:8000

# Development mode flag (automatically set by Vite)
# VITE_MODE=development
