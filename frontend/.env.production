# Production Environment Variables for Frontend
# These are loaded in production builds

# Backend API base URL - uses relative path in production (reverse-proxied)
# If you need a different domain, set this to your production API URL
# VITE_API_URL=/api

# WebSocket base URL - uses relative path in production (reverse-proxied)
# If you need a different domain, set this to your production WebSocket URL
# VITE_WS_URL=

# Production mode flag (automatically set by Vite)
# VITE_MODE=production
