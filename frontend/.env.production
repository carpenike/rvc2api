# Production Environment Variables for Frontend
# These are loaded in production builds

# Backend API base URL - explicitly set to empty string so endpoints use full paths
# This ensures /api/entities works correctly (endpoints already include /api prefix)
VITE_API_URL=

# WebSocket base URL - uses relative path in production (reverse-proxied)
# This allows WebSocket connections to work with /ws prefix
VITE_WS_URL=

# Production mode flag (automatically set by Vite)
# VITE_MODE=production
